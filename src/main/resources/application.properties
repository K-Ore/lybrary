spring.application.name=lybrary

# Server
server.port=${PORT:8080}

# Database (PostgreSQL)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/lybrary_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:lybrary_db_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:6ONo0VCuIAD3UmDfHOg0Zybb8Ou8SgMS}

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# JWT
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000
